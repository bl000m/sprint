

<%= project.name %>

<%= simple_form_for(project) do |f| %>
  <%= f.input :name, as: :hidden %>
  <%= f.input :trello_board_id, as: :hidden %>
  <p>Choissisez votre liste des trcus à faire</p>
  <%= f.select :trello_list_id, trello_lists.map {|v| [v['name'], v['id']]} %>
  <%#= f.select :trello_board_id, trello_lists.map {|v| [v['name'], v['id']]} %>
  <%# <p>Choissisez votre liste quand c'est fini</p> %>
  <%= f.submit %>
<% end %>
